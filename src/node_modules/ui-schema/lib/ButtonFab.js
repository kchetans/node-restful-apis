let {WxComponents, WebHookConstants} =  require('../constants');
let BaseActionContainer                    = require('./base/BaseActionContainer');

class ButtonFab extends BaseActionContainer {

    static init(param) {
        return new ButtonFab(param);
    }

    constructor(param) {
        super(param);
        this.config = {'type': WxComponents.BUTTON_FAB};
    }

}

module.exports = ButtonFab;
