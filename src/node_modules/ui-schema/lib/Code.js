let {WxComponents, WebHookConstants} =  require('../constants');
let BaseComponent                    = require('./base/BaseComponent');

class Code extends BaseComponent {

    static init(param) {
        return new Code(param);
    }

    constructor(param) {
        super(param);
        this.config = {'type': WxComponents.CODE};
    }

}

module.exports = Code;
