let {WxComponents, WebHookConstants} =  require('../constants');
let BaseActionContainer              = require('./base/BaseActionContainer');

class ButtonFlat extends BaseActionContainer {

    static init(param) {
        return new ButtonFlat(param);
    }

    constructor(param) {
        super(param);
        this.config = {'type': WxComponents.BUTTON_ICON};
    }

}

module.exports = ButtonFlat;
