let {WxComponents}     =  require('../constants');
let BaseContainer      = require('./base/BaseContainer');

class Actions extends BaseContainer {

    static init(param) {
        return new Actions(param);
    }

    constructor(param) {
        super(param);
        this.config = {'type': WxComponents.ACTIONS};
    }
}

module.exports = Actions;
