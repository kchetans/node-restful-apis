let {WxComponents, WebHookConstants} =  require('../constants');
let BaseComponent               = require('./base/BaseComponent');

class Array extends BaseComponent {

    static init(param) {
        return new Array(param);
    }

    constructor(param) {
        super(param);
        this.config = {'type': WxComponents.ARRAY};
    }

}

module.exports = Array;
