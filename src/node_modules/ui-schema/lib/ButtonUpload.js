let {WxComponents, WebHookConstants} =  require('../constants');
let BaseActionContainer              = require('./base/BaseActionContainer');

class ButtonUpload extends BaseActionContainer {

    static init(param) {
        return new ButtonUpload(param);
    }

    constructor(param) {
        super(param);
        this.config = {'type': WxComponents.BUTTON_UPLOAD};
    }
}

module.exports = ButtonUpload;
