let {WxComponents, WebHookConstants} =  require('../constants');
let BaseContainer                    = require('./base/BaseContainer');

class List extends BaseContainer {

    static init(param) {
        return new List(param);
    }

    constructor(param) {
        super(param);
        this.config = {'type': WxComponents.LIST};
    }

}

module.exports = List;
