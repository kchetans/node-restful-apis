let {WxComponents, WebHookConstants} =  require('../constants');
let BaseComponent               = require('./base/BaseComponent');

class Divider extends BaseComponent {

    static init(param) {
        return new Divider(param);
    }

    constructor(param) {
        super(param);
        this.config = {'type': WxComponents.DIVIDER};
    }

}

module.exports = Divider;
