let {WxComponents, WebHookConstants} =  require('../constants');
let BaseGroupComponent               = require('./base/BaseGroupComponent');

class Menu extends BaseGroupComponent {

    static init(param) {
        return new Menu(param);
    }

    constructor(param) {
        super(param);
        this.config = {'type': WxComponents.MENU};
    }

}

module.exports = Menu;
