var $ = require('_includes');

class PageManager{

  static instance(){
    var _instance = undefined;
    if(_instance){
      return _instance;
    }else{
      _instance = new PageManager();
      return _instance;
    }
  }

  getConfig(object, options){
    $.mdb.collection('users')
      .find({}) //id:object.pageId
      .toArray(function(err,docs){
        if(err){
          console.log(err);
        }else{
          console.log(docs);
          return docs;
        }
      });
  }
}

module.exports = PageManager;
